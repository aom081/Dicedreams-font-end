import React, { useState, useEffect } from "react";
import axios from "axios";
import {
  Box,
  Typography,
  Avatar,
  CircularProgress,
  Alert,
  IconButton,
  Menu,
  MenuItem,
  Button,
} from "@mui/material";
import { useNavigate } from "react-router-dom";

const Store = () => {
  return (
    <Box sx={{ marginTop: 8 }}>
      <Box sx={{ marginTop: 8 }}>sss</Box>
    </Box>
  );
};

export default Store;
